The mv command allows you to move files or directories from one place to another. 

It also allows for renaming of files by by 'moving' them into the file they are already situated and specifying their new filename. 
